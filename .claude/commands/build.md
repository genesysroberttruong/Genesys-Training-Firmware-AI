---
description: Build the firmware and report any warnings or errors
---
Run `make` and summarise the result.

If there are warnings or errors, list each one with its `file:line` and a
one-line explanation, then propose fixes. Do not change any code unless I ask
you to.
